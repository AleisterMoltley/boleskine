{
  "name": "boleskine",
  "private": true,
  "version": "1.0.0",
  "description": "Boleskine — play Aleister Crowley in a Tim Burton nightscape around Loch Ness.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "three": "^0.170.0",
    "vite": "^6.0.3"
  }
}
